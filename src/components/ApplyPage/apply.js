import React, { Component } from "react";

export default class Apply extends Component{
    render(){
        return(
            <div>
                <p>Apply!</p>
            </div>
        );
    }
}